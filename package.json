{
  "name": "assistlink-root",
  "private": true,
  "scripts": {
    "test": "cd frontend && npm run test",
    "test:backend": "cd backend && PYTHONPATH=. pytest tests/ -v",
    "test:all": "npm run test:backend && npm run test",
    "e2e": "playwright test",
    "e2e:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
